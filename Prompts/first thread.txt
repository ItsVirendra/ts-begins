I am a QA Automation Engineer (10 years experience) rebuilding my technical depth in TypeScript and Playwright.

I want you to continue mentoring me based on the progress made in a previous conversation.

Mentor Style:

* Strict and direct.
* Concise answers.
* No motivational speeches.
* No excessive theory.
* No spoon-feeding.
* Challenge my assumptions.
* Make me think before coding.
* Focus on practical implementation and code reviews.

Important:

* My biggest issue is not logic.
* My biggest issue is converting logic into code confidently.
* I often freeze when starting from a blank file.
* I tend to overthink architecture before writing code.
* I have relied heavily on existing automation frameworks and copied patterns rather than building things from scratch.

Current Learning Goal:
Become strong and reliable in Playwright + TypeScript through practical repetition.

What I learned during the previous discussion:

1. Arrays vs Objects

* A users collection should be stored in an array.
* A single user should be represented as an object.

2. Functions

* Functions should do one responsibility.
* Avoid unnecessary console.log statements inside utility functions.
* Return data instead of printing data.

3. Function Parameters
   Originally I hardcoded values inside functions.

Example:

```ts
const sampleEmail = "test@test.com";
```

I changed to:

```ts
getUserByEmail(emailInput: string)
```

4. Return Values
   I initially wanted to return arrays everywhere because I often use:

```ts
if (result.length > 0)
```

We discussed that:

```ts
getUserByEmail()
```

should return:

```ts
User | undefined
```

because email is unique and only one user should exist.

5. Loops
   I learned about:

```ts
return early when match is found
```

instead of storing unnecessary flags.

6. Equality
   I incorrectly thought:

```ts
==
```

was better because of case-insensitive comparison.

I learned:

```ts
===
```

should be the default.

7. Arrays
   I discovered:

* push()
* splice()

I initially wrote:

```ts
users_list.splice(i);
```

which removes everything from index i onward.

I learned:

```ts
splice(index, count)
```

and fixed it.

8. QA Mindset Lesson
   I learned that:

```text
No compile error
```

does NOT mean:

```text
Correct behavior
```

I should verify behavior after implementing logic.

Current Project:
Simple in-memory User Manager.

Current Functions:

```ts
function addUser(emailId: string, name: string, role: string) {
    if (!getUserByEmail(emailId)) {
        const current_length = users_list.length;
        const user_details = {
            name: name,
            email: emailId,
            role: role,
            id: current_length + 1,
            active: true
        };
        users_list.push(user_details);
        return true;
    } else {
        return false;
    }
}

function getUserByEmail(emailInput: string) {
    for (let i = 0; i < users_list.length; i++) {
        if (users_list[i].email === emailInput) {
            return users_list[i];
        }
    }
    return undefined;
}

function getActiveUsers() {
    let active_users = [];
    for (let i = 0; i < users_list.length; i++) {
        if (users_list[i].active) {
            active_users.push(users_list[i]);
        }
    }
    return active_users;
}

function updateUser(email: string, role: string, name: string) {
    for (let i = 0; i < users_list.length; i++) {
        if (users_list[i].email === email) {
            users_list[i].name = name;
            users_list[i].role = role;
            return true;
        }
    }
    return false;
}

function removeUser(email: string) {
    for (let i = 0; i < users_list.length; i++) {
        if (users_list[i].email === email) {
            users_list.splice(i, 1);
            return true;
        }
    }
    return false;
}
```

Project Checklist Status:

Completed:

* Create users array
* getUserByEmail()
* addUser()
* getActiveUsers()
* updateUser()
* removeUser()
* Function parameters
* Return values
* Basic array operations

Next Topic:
My mentor said CRUD is essentially complete.

The next learning step is:

```ts
type User = {
    id: number;
    name: string;
    email: string;
    role: string;
    active: boolean;
}
```

or an interface equivalent.

The goal is to start learning actual TypeScript type safety instead of relying on VS Code suggestions.

Continue from this point.
